---
import { getCollection } from 'astro:content';
import Layout from '@/layouts/Layout.astro';
import BlogPost from '@/components/BlogPost.astro';
import { Breadcrumb } from '@/components/ui/breadcrumb';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<Layout
  title="Blog | Abdelkbir Nainiaa - Software Engineering Insights"
  description="Read articles about web development, software engineering, modern frameworks, and personal projects by Abdelkbir Nainiaa."
  canonical="https://www.abdelkbirnainiaa.me/blog"
  type="website"
>
	<main class="container my-24 px-5">
		<Breadcrumb 
			client:load
			items={[
				{ label: "Home", href: "/" },
				{ label: "Blog" }
			]}
		/>
		<h1 class="text-4xl font-bold mb-8">My Blog</h1>
		<p class="text-lg mb-12">I write about web development, software engineering, and my personal projects.</p>
		<section>
			<ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
				{posts.map((post) => <BlogPost post={post} />)}
			</ul>
		</section>
	</main>
</Layout>
